services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: doc_ocr
      POSTGRES_USER: doc_ocr
      POSTGRES_PASSWORD: doc_ocr
    ports:
      - "5433:5432"
    volumes:
      - doc-ocr-postgres-data:/var/lib/postgresql/data

volumes:
  doc-ocr-postgres-data:
